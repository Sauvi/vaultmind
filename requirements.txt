# VaultMind v1.2 — requirements.txt
# Install with: pip install -r requirements.txt

# ── Core web framework ────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
jinja2==3.1.4

# ── Document parsing ──────────────────────────────────────────
pymupdf==1.24.5           # PDF text extraction (digital PDFs)
python-docx==1.1.2        # Word document support

# ── OCR (scanned PDFs — Hindi + English) ─────────────────────
pytesseract==0.3.13       # Python wrapper for Tesseract OCR
Pillow==10.3.0            # Image processing for OCR
pdf2image==1.17.0         # Convert PDF pages to images for OCR
# NOTE: Also install Tesseract binary:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu:  sudo apt install tesseract-ocr tesseract-ocr-hin poppler-utils
#   macOS:   brew install tesseract tesseract-lang poppler

# ── Semantic RAG ──────────────────────────────────────────────
sentence-transformers==3.0.1  # Semantic embeddings (all-MiniLM-L6-v2, 80MB)
numpy==1.26.4                 # Vector math for cosine similarity
# Model downloads automatically on first use (~80MB, one time)

# ── Utilities ─────────────────────────────────────────────────
pathlib2==2.3.7.post1     # Path utilities (Windows compat)
